#include<stdio.h>
void vowel_check(){
   char ch = s;
   if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u' )
   printf("character is vowel");
   else
   printf("character is consonents");

}
void main(){
  vowel_check();
}
