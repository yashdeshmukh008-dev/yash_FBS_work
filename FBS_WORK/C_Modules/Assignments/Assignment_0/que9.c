#include <stdio.h>

void main() {
    float base = 10.0, height = 8.0;
    float area;

    area = 0.5 * base * height;

    printf("Base = %.2f\n", base);
    printf("Height = %.2f\n", height);
    printf("Area of Triangle = %.2f\n", area);

    
}
